[services.backend]
name = "Dashboard Backend"
serviceType = "node"
root = "server"
buildCommand = "npm install"
startCommand = "node server.js"

[services.backend.environments]
PORT = "5000"
NODE_ENV = "production"
